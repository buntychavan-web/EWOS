apiVersion: v1
kind: ConfigMap
metadata:
  name: ewos-backend-config
  namespace: ewos
data:
  SPRING_PROFILES_ACTIVE: "prod"
  SERVER_PORT: "8080"
  DB_POOL_MAX: "20"
  DB_POOL_MIN: "5"
  REDIS_HOST: "ewos-redis"
  REDIS_PORT: "6379"
  APP_MESSAGING_KAFKA_ENABLED: "false"
  JWT_ISSUER: "ewos"
  JWT_ACCESS_TTL: "15m"
  JWT_REFRESH_TTL: "7d"
  ADMIN_USERNAME: "admin"
  ADMIN_EMAIL: "admin@ewos.local"
  PASSWORD_MIN_LENGTH: "8"
  PASSWORD_MAX_LENGTH: "128"
  PASSWORD_REQUIRE_UPPERCASE: "true"
  PASSWORD_REQUIRE_LOWERCASE: "true"
  PASSWORD_REQUIRE_DIGIT: "true"
  PASSWORD_REQUIRE_SPECIAL: "true"
  PASSWORD_HISTORY_SIZE: "5"
  AUTH_LOCKOUT_ENABLED: "true"
  AUTH_LOCKOUT_MAX_ATTEMPTS: "5"
  AUTH_LOCKOUT_DURATION: "15m"
  AUTH_RATELIMIT_ENABLED: "true"
  AUTH_RATELIMIT_MAX_ATTEMPTS: "60"
  AUTH_RATELIMIT_WINDOW: "1m"
  # Set to your real frontend origin(s) — a wildcard is rejected at startup
  # in the prod profile (see ADR-0004 / CorsConfig).
  APP_CORS_ALLOWED_ORIGINS: "https://app.example.com"
  APP_CORS_ALLOW_CREDENTIALS: "true"
  APP_CORS_MAX_AGE: "1h"
  APP_PURGE_ENABLED: "false"
