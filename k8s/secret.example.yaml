# Template only — do NOT apply this file as-is or commit real values.
# Copy to secret.yaml (already .gitignored — see below), fill in real
# values, and apply that instead:
#   kubectl apply -f k8s/secret.yaml
#
# Or generate imperatively without ever writing secrets to disk:
#   kubectl create secret generic ewos-backend-secret -n ewos \
#     --from-literal=SPRING_DATASOURCE_URL='jdbc:postgresql://<host>:5432/ewos' \
#     --from-literal=SPRING_DATASOURCE_USERNAME='ewos' \
#     --from-literal=SPRING_DATASOURCE_PASSWORD='<strong-password>' \
#     --from-literal=JWT_SECRET="$(openssl rand -base64 48)" \
#     --from-literal=ADMIN_PASSWORD='<strong-password>'
apiVersion: v1
kind: Secret
metadata:
  name: ewos-backend-secret
  namespace: ewos
type: Opaque
stringData:
  SPRING_DATASOURCE_URL: "jdbc:postgresql://CHANGE-ME:5432/ewos"
  SPRING_DATASOURCE_USERNAME: "ewos"
  SPRING_DATASOURCE_PASSWORD: "CHANGE-ME"
  # Generate with: openssl rand -base64 48 — refused at boot if placeholder-
  # like or under 32 bytes (see JwtSecretGuard).
  JWT_SECRET: "CHANGE-ME"
  # Refused at boot if this is still the ChangeMe!Admin123 dev/test
  # placeholder (see AdminPasswordGuard).
  ADMIN_PASSWORD: "CHANGE-ME"
