package com.ewos.ats.domain;

/** How a candidate's data-processing consent was captured. */
public enum CandidateConsentSource {
    APPLICATION_FORM,
    MANUAL_ENTRY,
    REFERRAL,
    AGENCY,
    IMPORTED,
    OTHER
}
