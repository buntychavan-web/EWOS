package com.ewos.ats.domain;

/** Where the candidate originated. */
public enum CandidateSource {
    REFERRAL,
    JOB_BOARD,
    AGENCY,
    CAMPUS,
    DIRECT,
    INTERNAL,
    WEBSITE,
    LINKEDIN,
    SOCIAL_MEDIA,
    EVENT,
    WALK_IN,
    OTHER
}
