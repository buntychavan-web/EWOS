package com.ewos.ats.domain;

/** Channel through which a candidate communication occurred. */
public enum CommunicationChannel {
    EMAIL,
    PHONE,
    SMS,
    WHATSAPP,
    IN_PERSON,
    VIDEO,
    LINKEDIN,
    OTHER
}
