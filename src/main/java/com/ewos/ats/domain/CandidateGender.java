package com.ewos.ats.domain;

/** Candidate gender. Optional; stored to support diversity reporting. */
public enum CandidateGender {
    MALE,
    FEMALE,
    NON_BINARY,
    PREFER_NOT_TO_SAY,
    OTHER
}
