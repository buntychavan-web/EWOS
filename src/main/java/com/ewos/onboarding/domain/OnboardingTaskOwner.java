package com.ewos.onboarding.domain;

/** Who is expected to drive an onboarding task to completion. */
public enum OnboardingTaskOwner {
    EMPLOYEE,
    MANAGER,
    BUDDY,
    HR,
    IT,
    ADMIN,
    OTHER
}
