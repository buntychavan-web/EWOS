package com.ewos.interview.domain;

/** Role a panel member plays on a specific interview round. */
public enum InterviewParticipantRole {
    INTERVIEWER,
    PANEL_LEAD,
    OBSERVER,
    COORDINATOR
}
