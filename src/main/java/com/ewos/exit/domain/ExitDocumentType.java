package com.ewos.exit.domain;

/** Type of document issued during exit. */
public enum ExitDocumentType {
    ACCEPTANCE_LETTER,
    RELIEVING_LETTER,
    EXPERIENCE_LETTER,
    SERVICE_CERTIFICATE,
    FNF_STATEMENT,
    PF_STATEMENT,
    OTHER
}
