package com.ewos.performance.domain;

/** Promotion recommendation attached to a finalised appraisal. */
public enum PromotionRecommendation {
    NONE,
    READY_NOW,
    READY_NEXT_CYCLE,
    NOT_READY
}
