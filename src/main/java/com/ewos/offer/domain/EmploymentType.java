package com.ewos.offer.domain;

/** Employment type on an offer. Matches the recruitment / ATS values. */
public enum EmploymentType {
    FULL_TIME,
    PART_TIME,
    CONTRACT,
    TEMPORARY,
    INTERN,
    CONSULTANT
}
